  <!-- Main Sidebar Container -->
  <aside class="main-sidebar sidebar-dark-primary elevation-4">
    <!-- Brand Logo -->
    <a href="index3.html" class="brand-link">
      <!-- <img src="dist/img/AdminLTELogo.png" alt="AdminLTE Logo" class="brand-image img-circle elevation-3" style="opacity: .8"> -->
      <span class="brand-text font-weight-light">AdminLTE 3</span>
    </a>

    <!-- Sidebar -->
    <div class="sidebar">
      <!-- Sidebar user panel (optional) -->
      <% if current_user %>
      <div class="user-panel mt-3 pb-3 mb-3 d-flex">
        <div class="image">
          <!-- <img src="dist/img/user2-160x160.jpg" class="img-circle elevation-2" alt="User Image"> -->
        </div>
        <div class="info">
          <%= link_to current_user.email, class: "d-block" %>       
          </div>
      </div>
       <% end %>
      <!-- Sidebar Menu -->
      <nav class="mt-2">
        <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
          <!-- Add icons to the links using the .nav-icon class
               with font-awesome or any other icon font library -->
          <li class="<%= "nav-link mt-4 active" if current_page?(root_path) %>">
            <a href="<%= root_path %>" class="nav-link">
              <i class="nav-icon far fa-calendar-alt"></i>
              <p>
                Anasayfa
              </p>
            </a>
          </li>
         <li class="<%= "nav-link active" if current_page?(users_index_path) %>">
            <a href="<%= users_index_path %>" class="nav-link">
              <i class="nav-icon far fa-calendar-alt"></i>
              <p>
                Kullanıcılar
              </p>
            </a>
          </li>
          <li class="<%= "nav-link active" if current_page?(products_path) %>">
            <a href="<%= products_path %>" class="nav-link">
              <i class="nav-icon fas fa-glass-whiskey"></i>
              <p>
                Ürünler
              </p>
            </a>
          </li>
            <li class="<%= "nav-link active" if current_page?(companies_path) %>">
            <a href="<%= companies_path %>" class="nav-link">
              <i class="nav-icon fas fa-city"></i>
              <p>
                Firmalar
              </p>
            </a>
          </li>
            <li class=" <%= "nav-link active" if current_page?(bids_path) %>">
            <a href="<%= bids_path %>" class="nav-link">
              <i class="nav-icon fas fa-dollar-sign"></i>
              <p>
                İhaleler
              </p>
            </a>
          </li>
          <li class=" <%= "nav-link active" if current_page?(units_path) %> ">
            <a href="<%= units_path %>" class="nav-link">
              <i class="nav-icon fas fa-city"></i>
              <p>
                Birim 
              </p>
            </a>
          </li>
        </ul>
      </nav>
      <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
  </aside>